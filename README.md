---
type: project
---

# comparativa

All-Python reference pipeline that parses Fountain screenplays and generates
episode audio with MLX speech models, so its output can be compared under
controlled conditions against the existing Swift stack (Produciesta /
SwiftVoxAlta). The Swift stack is measured, never modified.

## Requirements

- Python 3.12 (managed by [`uv`](https://docs.astral.sh/uv/))
- `mlx-audio==0.4.8` — the version validated by the smoke test
- Apple Silicon (MLX)

## Setup

```sh
uv sync
uv run comparativa --help
```

## Commands

| Command | Purpose |
|---------|---------|
| `parse` | Parse a Fountain screenplay into a JSON element stream |
| `voices` | Build the character roster and per-engine voice assignments (`--audition` generates a fixed sentence per character × engine) |
| `generate` | Generate episode audio for one engine condition |
| `bench` | Run the benchmark matrix across conditions and episodes |
| `listen` | Build a blinded listening set and scoring sheet |
| `report` | Tabulate metrics and scores into a report |

All six subcommands are implemented.

```sh
# Generate an episode (offline; all checkpoints are cached locally)
HF_HUB_OFFLINE=1 uv run comparativa generate \
    ~/Projects/podcasts/granville/episodes/episode_1_01_cold_open.fountain \
    --engine qwen3-0.6b -o out/
```

## Tests

```sh
uv run pytest            # fast unit suite, no checkpoints loaded
uv run pytest --smoke    # + engine smoke and the full-episode integration test
```

## Planning documents

- `REQUIREMENTS.md` — locked requirements
- `EXECUTION_PLAN.md` — mission plan and sortie breakdown
- `REPORT.md` — benchmark report (generated by `comparativa report`)
- `docs/TEXT_PREP.md` — TTS text preparation and its Swift-stack parity
- `docs/SAMPLING_PARITY.md` — sampling parameters traced to `GenerationSettings.swift`
- `docs/ASSEMBLY.md` — episode assembly, loudness, and the `manifest.json` schema
- `docs/BENCH.md` — benchmark matrix, conditions, and metrics schema
- `docs/CORPUS_PIN.md` — the frozen corpus snapshot the tests are pinned to
- `docs/SWIFT_BASELINE.md` — how the Swift baseline (condition A) is captured
- `docs/CONDITION_F_RECON.md` — condition F reconnaissance notes
- `docs/EASE_OF_USE.md` — ease-of-use comparison notes

## Agent instructions

Agent guidance lives in [`AGENTS.md`](AGENTS.md); `CLAUDE.md` is a symlink to it.
